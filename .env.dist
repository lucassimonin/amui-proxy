# Proxy partagé — à copier en .env (non versionné) : cp .env.dist .env
#
# Email pour Let's Encrypt (avis d'expiration de certificat)
ACME_EMAIL=hello@amui.fr

# Un domaine par projet — chacun DOIT pointer (DNS A) sur l'IP du serveur.
AMUI_DOMAIN=amuï.com
CHEZMARCEL_DOMAIN=chezmarcel-palavas.fr

